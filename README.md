# marketplace-invoices

Deploy repo for the **invoices** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs invoices --push`.

Coolify: base_directory `/`, dockerfile `/apps/invoices/Dockerfile`.
